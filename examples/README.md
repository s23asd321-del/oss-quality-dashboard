# Examples

This directory contains safe sample repositories for local testing.

- `sample-good-repo` includes most expected open source project quality files.
- `sample-risky-repo` intentionally includes fake demo files with risky names so the scanner can show findings.

The risky sample files do not contain real secrets, real credentials, real emails, real IP addresses, or real logs.

