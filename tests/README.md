# Tests

The test suite covers the SQLite layer, built-in rules, scanner behavior, scoring, report generation, API endpoints, CLI output, and static dashboard serving.

Run:

```bash
python -m pytest
```

